# -*- coding: utf-8 -*-
# vim: ft=yaml
# Defaults for fail2ban

fail2ban:
  enabled: False
  pkg: fail2ban
  service:
    name: fail2ban
    state: dead
    enable: False
  config: {}
  jail_conf: {}
  jail_local: {}
  default: {}
  actiond: {}
  jaild: {}
  filters: {}
